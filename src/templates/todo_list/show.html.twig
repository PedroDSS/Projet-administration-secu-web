{% extends 'base.html.twig' %}

{% block title %}Administration Reseau | Tâche{% endblock %}

{% block body %}
    <h1>Tâche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ todo_list.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ todo_list.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ todo_list.description }}</td>
            </tr>
            <tr>
                <th>Done</th>
                <td>{{ todo_list.done ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_todo_list_index') }}">back to list</a>

    <a href="{{ path('app_todo_list_edit', {'id': todo_list.id}) }}">edit</a>

    {{ include('todo_list/_delete_form.html.twig') }}
{% endblock %}
